import React from 'react'

const TeamList = ({ size }) => {

    // items.items.forEach(item => {
    //  item.
      console.log(size)
    // });
    return (
        <div>
           <h4> {size} </h4>
        </div>
    )
}

export default TeamList